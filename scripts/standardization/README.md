# scripts/standardization

Scripts in this folder standardize downloaded variables to repository conventions (units, names, metadata, and format expectations).

## What it contains
- Dataset-specific standardization recipes (currently focused on selected datasets).

## Role in the workflow
- Harmonizes raw fields before or alongside derived/interpolated processing so outputs are consistent across catalogues and validations.
