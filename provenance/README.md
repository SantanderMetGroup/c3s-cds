# provenance

This folder stores provenance JSON files for supported datasets.

## What it contains
- One JSON per dataset (for example `reanalysis-era5-single-levels.json`).
- Variable-level metadata and source/provenance definitions.

## Role in the workflow
- Provides traceability for variables and derived fields.
- Supports consistent metadata documentation across catalogues and validations.
